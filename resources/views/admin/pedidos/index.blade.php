@extends('layouts.admin')
@php($title = 'Pedidos')
@section('content')
<div class="d-flex flex-wrap gap-2 mb-3"><a href="{{ route('admin.pedidos.index') }}" class="btn btn-sm {{ $status === '' ? 'btn-dark' : 'btn-outline-dark' }}">Todos</a>@foreach(\App\Http\Controllers\Admin\OrderController::STATUSES as $key => [$label, $color])<a href="{{ route('admin.pedidos.index', ['estado' => $key]) }}" class="btn btn-sm {{ $status === $key ? 'btn-'.$color : 'btn-outline-'.$color }}">{{ $label }}</a>@endforeach</div><div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Pedido</th><th>Cliente</th><th>Pago</th><th>Total</th><th>Estado</th><th>Fecha</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>{{ $order->numero }}</strong></td><td>{{ $order->cliente_nombre }}<br><small class="text-muted">{{ $order->cliente_email }}</small></td><td>{{ $order->metodo_pago }}<br><span class="badge bg-{{ $order->pago_estado === 'pagado' ? 'success' : ($order->pago_estado === 'fallido' ? 'danger' : 'warning text-dark') }}">{{ ucfirst($order->pago_estado) }}</span></td><td>${{ number_format($order->total, 2) }} {{ $currency }}</td><td><span class="badge bg-{{ \App\Http\Controllers\Admin\OrderController::STATUSES[$order->estado][1] }}">{{ \App\Http\Controllers\Admin\OrderController::STATUSES[$order->estado][0] }}</span></td><td>{{ $order->creado_en->format('d/m/Y H:i') }}</td><td><a href="{{ route('admin.pedidos.show', $order) }}" class="btn btn-sm btn-primary">Ver</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">No hay pedidos.</td></tr>@endforelse</tbody></table></div></div>
@endsection
