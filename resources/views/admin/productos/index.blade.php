@extends('layouts.admin')
@php($title = 'Productos')
@section('content')
<div class="d-flex flex-wrap gap-2 justify-content-between mb-3"><form class="d-flex gap-2" method="get"><input type="search" name="q" class="form-control" placeholder="Buscar producto…" value="{{ $search }}"><button class="btn btn-outline-secondary">Buscar</button></form><a href="{{ route('admin.productos.create') }}" class="btn btn-primary"><i class="bi bi-plus-circle me-1"></i>Nuevo producto</a></div><div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th></th><th>Producto</th><th>Plataforma</th><th>Precio proveedor</th><th>Precio venta</th><th>Estado</th><th class="text-end">Acciones</th></tr></thead><tbody>@forelse($products as $product)<tr><td style="width:60px"><img src="{{ $product->imageUrl() }}" style="width:50px;height:50px;object-fit:cover" class="rounded"></td><td><strong>{{ str($product->nombre)->limit(60) }}</strong><br><a href="{{ $product->url_original }}" target="_blank" rel="noopener" class="small text-muted">Link original <i class="bi bi-box-arrow-up-right"></i></a></td><td><span class="badge bg-dark">{{ ucfirst($product->plataforma) }}</span></td><td>${{ number_format($product->precio_original, 2) }} {{ $currency }}</td><td><strong>${{ number_format($product->precioFinal(), 2) }} {{ $currency }}</strong>@if($product->precio_manual)<span class="badge bg-secondary">manual</span>@endif</td><td><span class="badge bg-{{ $product->activo ? 'success' : 'secondary' }}">{{ $product->activo ? 'Publicado' : 'Inactivo' }}</span></td><td class="text-end"><a href="{{ route('admin.productos.edit', $product) }}" class="btn btn-sm btn-outline-primary" title="Editar"><i class="bi bi-pencil"></i></a><form method="post" action="{{ route('admin.productos.toggle', $product) }}" class="d-inline">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-{{ $product->activo ? 'warning' : 'success' }}" title="{{ $product->activo ? 'Desactivar' : 'Activar' }}"><i class="bi bi-{{ $product->activo ? 'eye-slash' : 'eye' }}"></i></button></form><form method="post" action="{{ route('admin.productos.destroy', $product) }}" class="d-inline" onsubmit="return confirm('¿Eliminar este producto definitivamente?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="Eliminar"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">No hay productos todavía.</td></tr>@endforelse</tbody></table></div></div>
@endsection
